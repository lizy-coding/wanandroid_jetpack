# Module Workflow: testing-quality

Scope derived from `LEARNING_PLAN.md` skeleton or project module analysis:
- Unit and instrumented testing
- `app/src/test/`, `app/src/androidTest/`

Steps:
1. Read `app/src/test/` and `app/src/androidTest/` plus any test utilities.
2. Map to interview topics: unit vs instrumented, test doubles, Espresso basics, CI hooks.
3. Pull job requirements using `scripts/fetch_job_requirements.sh` and summarize with `scripts/summarize_job_requirements.sh`.
   - Skip this step unless the user asks for interview/job-market alignment.
   - If blocked, request 3-5 recent job posts.
4. Produce output using `references/module-shared-format.md`.

# Module Workflow: mvvm-base

Scope derived from `LEARNING_PLAN.md` skeleton:
- MVVM base conventions in `base/`

Steps:
1. Read `app/src/main/java/com/yechaoa/wanandroid_jetpack/base/`.
2. Identify base Activity/Fragment/ViewModel responsibilities and shared patterns.
3. Map to interview topics: lifecycle, state management, one-shot events, error handling, ViewModel scope.
4. Pull job requirements using `scripts/fetch_job_requirements.sh` and summarize with `scripts/summarize_job_requirements.sh`.
   - If blocked, request 3-5 recent job posts.
5. Produce output using `references/module-shared-format.md`.
